import './components/app-shell.js';

// We are importing the app-shell component which is the entry point of our application
// The app-shell component initializes the router and renders the main layout

// This file serves as the entry point for our application
// The rest of the components are lazily loaded by the router when needed
